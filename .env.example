# OpenAI / Groq API Key
GROQ_API_KEY=your_groq_api_key_here

# Google Cloud Credentials (JSON content pasted as string, or path)
# RECOMMENDED: Use base64 encoded string if possible for easier deployment
GOOGLE_CREDENTIALS={"type": "service_account", ...}

# Gmail API Token (Base64 Encoded)
GMAIL_TOKEN=eyX... (Paste the long string here)
# FROM_EMAIL is optional, defaults to your authenticated Gmail
# FROM_EMAIL=your_email@gmail.com
